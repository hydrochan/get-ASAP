---
phase: 04-notion
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - notion_client_mod.py
autonomous: false
requirements:
  - NOTION-01
  - NOTION-02
  - NOTION-03

must_haves:
  truths:
    - "실제 Notion API로 DB가 생성되거나 기존 DB에 연결된다"
    - "테스트 논문이 Notion DB에 페이지로 저장된다"
    - "동일 논문 재저장 시 중복 페이지가 생성되지 않는다"
  artifacts:
    - path: "notion_client_mod.py"
      provides: "실제 API 연동 검증된 Notion 클라이언트"
  key_links:
    - from: "notion_client_mod.py"
      to: "Notion API"
      via: "notion-client 3.0.0 SDK"
      pattern: "get_notion_client"
---

<objective>
notion_client_mod.py를 실제 Notion API로 통합 테스트한다. mock이 아닌 실제 API 호출로 DB 생성, 페이지 저장, 중복 방지가 동작하는지 검증한다.

Purpose: 단위 테스트만으로는 SDK 3.0.0 breaking changes 대응 여부를 확신할 수 없음
Output: 실제 Notion DB에 테스트 데이터 저장 확인
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-notion/04-CONTEXT.md
@.planning/phases/04-notion/04-RESEARCH.md
@.planning/phases/04-notion/04-01-SUMMARY.md

@notion_client_mod.py
@notion_auth.py
@config.py
@models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 실제 Notion API 통합 테스트 스크립트 작성 및 실행</name>
  <files>notion_client_mod.py</files>
  <read_first>
    - notion_client_mod.py (Plan 01에서 구현한 코드)
    - config.py (환경변수 확인)
    - .env.example (필요한 환경변수 목록)
  </read_first>
  <action>
    실제 Notion API로 통합 테스트를 수행하는 임시 스크립트를 작성하고 실행한다.

    테스트 순서:
    1. Notion 연결 확인: `verify_notion_connection` 호출
    2. DB 생성 또는 기존 DB 연결: `get_or_create_db()` 호출
    3. data_source_id 획득: `_get_data_source_id(db_id)` 호출
    4. 테스트 논문 저장: `save_paper(test_paper, db_id, ds_id)` 호출
       - test_paper: title="통합 테스트 논문", doi="10.9999/test.integration", journal="Test Journal", date="2025-01-01"
    5. 중복 저장 시도: 동일 paper로 save_paper 재호출 -> False 반환 확인
    6. save_papers 배치 테스트: 2-3개 논문 리스트 저장

    실행 중 발생하는 API 오류가 있으면 notion_client_mod.py를 수정하여 해결한다.
    특히 주의할 점:
    - initial_data_source 구조가 실제 API에서 받아들여지는지
    - data_sources.query 필터가 올바르게 동작하는지
    - Title 속성 필터 타입이 맞는지

    테스트 완료 후 임시 스크립트는 삭제한다.
    Notion에 생성된 테스트 데이터는 사용자가 수동으로 삭제하도록 안내한다.
  </action>
  <verify>
    <automated>cd "C:\GDrive_CK\내 드라이브\05_Dev\get-ASAP" && python -c "from notion_client_mod import get_or_create_db; print('import OK')"</automated>
    모듈 import가 성공해야 한다.
  </verify>
  <acceptance_criteria>
    - notion_client_mod.py가 실제 Notion API와 정상 통신
    - 기존 단위 테스트 회귀 없음: `python -m pytest tests/ -x -q` PASSED
  </acceptance_criteria>
  <done>실제 Notion API로 DB 생성/페이지 저장/중복 방지가 동작 확인됨</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Notion DB 실제 결과 확인</name>
  <files>notion_client_mod.py</files>
  <action>
    사용자가 Notion 워크스페이스에서 통합 테스트 결과를 직접 확인한다.
    확인 항목: DB 스키마, 테스트 페이지 저장 여부, 중복 방지 동작.
  </action>
  <verify>
    <automated>cd "C:\GDrive_CK\내 드라이브\05_Dev\get-ASAP" && python -m pytest tests/ -x -q</automated>
  </verify>
  <done>사용자가 Notion에서 결과를 확인하고 승인함</done>
  <what-built>
    notion_client_mod.py -- Notion DB 생성, 논문 페이지 저장, DOI 기반 중복 방지 모듈.
    실제 Notion API로 통합 테스트 완료.
  </what-built>
  <how-to-verify>
    1. Notion 워크스페이스에서 "get-ASAP Papers" DB가 생성되었는지 확인
    2. DB에 Title, DOI, Journal, Date, Status, URL, Authors 속성이 존재하는지 확인
    3. 테스트 논문 페이지가 저장되었는지 확인 (Status = "대기중")
    4. 중복 논문이 1개만 존재하는지 확인 (2번 저장 시도했지만 1개만 있어야 함)
    5. (선택) 테스트 데이터 삭제
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. Notion 워크스페이스에 "get-ASAP Papers" DB 존재
2. 테스트 논문 페이지가 올바른 속성으로 저장됨
3. 중복 저장이 방지됨
4. `python -m pytest tests/ -x -q` 전체 통과
</verification>

<success_criteria>
- 실제 Notion API 호출이 오류 없이 완료됨
- DB 생성, 페이지 저장, 중복 방지가 실제 환경에서 동작함
- 사용자가 Notion에서 결과를 직접 확인하고 승인함
</success_criteria>

<output>
After completion, create `.planning/phases/04-notion/04-02-SUMMARY.md`
</output>
