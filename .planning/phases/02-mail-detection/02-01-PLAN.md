---
phase: 02-mail-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.py
  - models.py
  - publishers.json
  - parsers/__init__.py
  - parsers/base.py
  - parser_registry.py
  - tests/test_models.py
  - tests/test_parser_registry.py
autonomous: true
requirements: [PARSE-04, PARSE-05]

must_haves:
  truths:
    - "PaperMetadata dataclass가 title, doi, journal, date 필드를 가진다"
    - "BaseParser ABC를 상속한 클래스가 can_parse, parse 추상 메서드를 구현해야 한다"
    - "parsers/ 디렉토리에 파일을 추가하면 parser_registry가 자동으로 발견하여 등록한다"
    - "publishers.json에서 발신자 이메일로 출판사와 저널명을 조회할 수 있다"
    - "config.py의 GMAIL_SCOPES가 gmail.modify로 확장되어 있다"
  artifacts:
    - path: "models.py"
      provides: "PaperMetadata dataclass"
      contains: "@dataclass"
    - path: "parsers/base.py"
      provides: "BaseParser ABC"
      contains: "class BaseParser(ABC)"
    - path: "parser_registry.py"
      provides: "parsers/ 자동 디스커버리"
      exports: ["load_parsers"]
    - path: "publishers.json"
      provides: "출판사 설정 (발신자, 이름, 저널)"
      contains: "alerts@acs.org"
    - path: "config.py"
      provides: "gmail.modify scope"
      contains: "gmail.modify"
  key_links:
    - from: "parsers/base.py"
      to: "models.py"
      via: "from models import PaperMetadata"
      pattern: "from models import PaperMetadata"
    - from: "parser_registry.py"
      to: "parsers/base.py"
      via: "BaseParser.__subclasses__()"
      pattern: "__subclasses__"
---

<objective>
Phase 2 데이터 계약 및 파서 플러그인 구조 구축

Purpose: gmail_client.py(Plan 02)가 의존할 핵심 데이터 모델(PaperMetadata), 출판사 설정(publishers.json), 파서 플러그인 자동 디스커버리 구조를 먼저 확립한다. config.py의 Gmail scope도 gmail.modify로 확장한다.
Output: models.py, parsers/base.py, parser_registry.py, publishers.json, 업데이트된 config.py, 단위 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-mail-detection/02-CONTEXT.md
@.planning/phases/02-mail-detection/02-RESEARCH.md

<interfaces>
<!-- 기존 코드에서 사용할 인터페이스 -->

From config.py:
```python
GMAIL_CREDENTIALS_PATH = os.getenv("GMAIL_CREDENTIALS_PATH", "credentials.json")
GMAIL_TOKEN_PATH = os.getenv("GMAIL_TOKEN_PATH", "token.json")
GMAIL_SCOPES = ["https://www.googleapis.com/auth/gmail.readonly"]  # -> gmail.modify로 변경 필요
NOTION_TOKEN = os.getenv("NOTION_TOKEN")
NOTION_DATABASE_ID = os.getenv("NOTION_DATABASE_ID")
CHECK_INTERVAL_HOURS = int(os.getenv("CHECK_INTERVAL_HOURS", "6"))
```

From tests/conftest.py:
```python
@pytest.fixture
def mock_env(monkeypatch):
    monkeypatch.setenv("GMAIL_CREDENTIALS_PATH", "test_credentials.json")
    monkeypatch.setenv("GMAIL_TOKEN_PATH", "test_token.json")
    monkeypatch.setenv("NOTION_TOKEN", "test_notion_token")
    monkeypatch.setenv("NOTION_DATABASE_ID", "test_db_id")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 데이터 모델 + 출판사 설정 + scope 확장</name>
  <files>models.py, publishers.json, config.py, tests/test_models.py</files>
  <read_first>config.py, tests/conftest.py, .planning/phases/02-mail-detection/02-RESEARCH.md</read_first>
  <behavior>
    - Test 1 (test_paper_metadata_creation): PaperMetadata(title="Test", doi="10.1234/test", journal="JACS", date="2026-04-03") 생성 시 모든 필드 접근 가능
    - Test 2 (test_paper_metadata_optional_fields): authors=None, url=None 기본값 확인
    - Test 3 (test_paper_metadata_with_authors): authors=["Kim", "Lee"], url="https://doi.org/..." 설정 가능
    - Test 4 (test_publishers_json_schema): publishers.json 로드 시 각 출판사에 sender, name, journals 키 존재
    - Test 5 (test_publishers_journal_lookup): publishers.json에서 sender로 출판사명/저널 조회 가능
  </behavior>
  <action>
    **models.py 생성 (per D-08):**
    ```python
    from dataclasses import dataclass, field
    from typing import Optional

    @dataclass
    class PaperMetadata:
        title: str
        doi: str
        journal: str
        date: str
        authors: Optional[list[str]] = field(default=None)
        url: Optional[str] = None
    ```

    **publishers.json 생성 (per D-02, D-06):**
    ```json
    {
      "acs": {
        "sender": "alerts@acs.org",
        "name": "ACS Publications",
        "journals": ["JACS", "ACS Nano", "ACS Catalysis", "Nano Letters"]
      },
      "elsevier": {
        "sender": "ealerts@elsevier.com",
        "name": "Elsevier",
        "journals": ["Applied Catalysis B", "Journal of Catalysis"]
      },
      "science": {
        "sender": "ScienceAdvances@sciencemag.org",
        "name": "Science",
        "journals": ["Science", "Science Advances"]
      }
    }
    ```
    NOTE: 발신자 이메일은 플레이스홀더 -- Phase 3 시작 전 실제 메일에서 확인 후 수정 필요.

    **config.py 수정 (per D-05):**
    GMAIL_SCOPES를 `["https://www.googleapis.com/auth/gmail.modify"]`로 변경.
    gmail.modify는 gmail.readonly의 상위 집합이므로 읽기+라벨 부여 모두 가능.
    주석으로 "Phase 2: gmail.modify로 확장 (라벨 부여를 위해)" 명시.

    **tests/test_models.py 생성:**
    위 behavior 5개 테스트를 pytest로 작성. publishers.json은 json.load로 직접 로드하여 스키마 검증.
  </action>
  <verify>
    <automated>cd "C:\GDrive_CK\내 드라이브\05_Dev\get-ASAP" && .venv\Scripts\python.exe -m pytest tests/test_models.py -v -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class PaperMetadata" models.py` 결과 1
    - `grep "gmail.modify" config.py` 매칭
    - `python -c "import json; d=json.load(open('publishers.json')); assert 'acs' in d and 'sender' in d['acs']"`
    - tests/test_models.py에서 5개 테스트 전부 PASSED
  </acceptance_criteria>
  <done>PaperMetadata dataclass 완성, publishers.json 3개 출판사 설정 완비, config.py scope가 gmail.modify로 확장, 5개 단위 테스트 통과</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BaseParser ABC + 자동 디스커버리 레지스트리</name>
  <files>parsers/__init__.py, parsers/base.py, parser_registry.py, tests/test_parser_registry.py</files>
  <read_first>models.py, .planning/phases/02-mail-detection/02-RESEARCH.md</read_first>
  <behavior>
    - Test 1 (test_base_parser_is_abstract): BaseParser()를 직접 인스턴스화하면 TypeError 발생
    - Test 2 (test_base_parser_requires_can_parse): can_parse 미구현 서브클래스 인스턴스화 시 TypeError
    - Test 3 (test_base_parser_requires_parse): parse 미구현 서브클래스 인스턴스화 시 TypeError
    - Test 4 (test_concrete_parser_instantiation): can_parse + parse 구현한 서브클래스는 정상 생성
    - Test 5 (test_auto_discovery): parsers/ 디렉토리에 DummyParser(BaseParser 서브클래스) 파일을 임시 생성 후 load_parsers() 호출 시 해당 파서가 목록에 포함
    - Test 6 (test_discovery_ignores_non_parser): parsers/ 디렉토리의 __init__.py, base.py, 언더스코어 시작 파일은 무시
  </behavior>
  <action>
    **parsers/__init__.py 생성:**
    빈 파일 (패키지 초기화).

    **parsers/base.py 생성 (per D-07, D-08):**
    ```python
    """출판사 파서 기저 클래스 (per D-07)"""
    from abc import ABC, abstractmethod
    from models import PaperMetadata

    class BaseParser(ABC):
        """출판사별 메일 파서의 추상 기저 클래스.
        parsers/ 디렉토리에 이 클래스를 상속한 파일을 추가하면 자동 등록된다."""
        publisher_name: str = ""

        @abstractmethod
        def can_parse(self, sender: str, subject: str) -> bool:
            """이 파서가 해당 메일을 처리할 수 있는지 판단."""

        @abstractmethod
        def parse(self, message_body: str) -> list[PaperMetadata]:
            """메일 본문에서 논문 메타데이터 목록 추출."""
    ```

    **parser_registry.py 생성 (per D-07):**
    ```python
    """파서 플러그인 자동 디스커버리 (per D-07)"""
    import importlib.util
    import os
    from parsers.base import BaseParser

    def load_parsers(parsers_dir: str = None) -> list[BaseParser]:
        """parsers/ 디렉토리의 모든 .py 파일을 로드하여 BaseParser 서브클래스 인스턴스 반환.
        __init__.py, base.py, _로 시작하는 파일은 무시한다."""
        if parsers_dir is None:
            parsers_dir = os.path.join(os.path.dirname(__file__), "parsers")
        for fname in sorted(os.listdir(parsers_dir)):
            if not fname.endswith(".py"):
                continue
            if fname.startswith("_") or fname == "base.py":
                continue
            path = os.path.join(parsers_dir, fname)
            spec = importlib.util.spec_from_file_location(fname[:-3], path)
            mod = importlib.util.module_from_spec(spec)
            spec.loader.exec_module(mod)
        return [cls() for cls in BaseParser.__subclasses__()]
    ```
    한국어 주석으로 importlib 디스커버리 흐름 설명 추가.

    **tests/test_parser_registry.py 생성:**
    위 behavior 6개 테스트 작성. Test 5는 tmp_path 픽스처로 임시 parsers 디렉토리를 만들고, DummyParser 클래스가 포함된 .py 파일을 동적 생성한 후 load_parsers(tmp_dir)를 호출. Test 5/6에서 BaseParser.__subclasses__() 캐시 문제를 피하기 위해 각 테스트 후 등록된 서브클래스를 정리하는 방법 고려 (또는 subprocess로 격리).
    
    중요: __subclasses__()는 프로세스 전역 상태이므로, 테스트 간 격리를 위해 각 테스트에서 load_parsers 호출 전 기존 서브클래스 수를 기록하고 새로 추가된 것만 검증하는 방식 사용.
  </action>
  <verify>
    <automated>cd "C:\GDrive_CK\내 드라이브\05_Dev\get-ASAP" && .venv\Scripts\python.exe -m pytest tests/test_parser_registry.py -v -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep "class BaseParser(ABC)" parsers/base.py` 매칭
    - `grep "def load_parsers" parser_registry.py` 매칭
    - `grep "abstractmethod" parsers/base.py` 매칭 2회 (can_parse, parse)
    - `grep "__subclasses__" parser_registry.py` 매칭
    - tests/test_parser_registry.py에서 6개 테스트 전부 PASSED
  </acceptance_criteria>
  <done>BaseParser ABC가 can_parse/parse 추상 메서드를 강제하고, parser_registry.py가 parsers/ 디렉토리를 자동 스캔하여 서브클래스를 등록하며, 6개 단위 테스트 통과</done>
</task>

</tasks>

<verification>
cd "C:\GDrive_CK\내 드라이브\05_Dev\get-ASAP" && .venv\Scripts\python.exe -m pytest tests/test_models.py tests/test_parser_registry.py -v

전체 11개 테스트 PASSED 확인.
</verification>

<success_criteria>
1. models.py에 PaperMetadata dataclass 정의 (title, doi, journal, date + optional authors, url)
2. publishers.json에 acs, elsevier, science 3개 출판사 설정 포함
3. config.py GMAIL_SCOPES가 gmail.modify로 변경됨
4. parsers/base.py에 BaseParser ABC (can_parse, parse 추상 메서드)
5. parser_registry.py의 load_parsers()가 parsers/ 디렉토리 자동 스캔
6. 11개 단위 테스트 전부 통과
</success_criteria>

<output>
After completion, create `.planning/phases/02-mail-detection/02-01-SUMMARY.md`
</output>
