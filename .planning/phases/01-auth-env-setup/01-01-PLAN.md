---
phase: 01-auth-env-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - config.py
  - .env.example
  - .gitignore
  - auth.py
  - verify_gmail.py
  - tests/conftest.py
  - tests/test_gmail_auth.py
autonomous: false
requirements:
  - AUTH-01
user_setup:
  - service: google-cloud
    why: "Gmail API OAuth 2.0 credentials 발급"
    env_vars:
      - name: GMAIL_CREDENTIALS_PATH
        source: "Google Cloud Console > APIs & Services > Credentials > OAuth 2.0 Client ID > Download JSON"
    dashboard_config:
      - task: "Gmail API 활성화"
        location: "Google Cloud Console > APIs & Services > Library > Gmail API > Enable"
      - task: "OAuth 동의 화면 설정 + 테스트 사용자 등록"
        location: "Google Cloud Console > APIs & Services > OAuth consent screen > Test users"
      - task: "Desktop App OAuth credential 생성 및 credentials.json 다운로드"
        location: "Google Cloud Console > APIs & Services > Credentials > Create Credentials > OAuth client ID > Desktop app"

must_haves:
  truths:
    - "프로젝트 루트에 venv를 생성하고 모든 의존성이 설치된다"
    - "config.py가 .env에서 환경변수를 로드하고 경로를 반환한다"
    - "auth.py의 get_gmail_service()가 OAuth 인증 후 Gmail API 서비스 객체를 반환한다"
    - "verify_gmail.py 실행 시 Gmail 라벨 목록이 출력된다"
    - "token.json이 자동 생성되고 만료 시 자동 갱신된다"
  artifacts:
    - path: "requirements.txt"
      provides: "Python 의존성 목록"
      contains: "google-api-python-client"
    - path: "config.py"
      provides: "환경변수 로드 및 경로 설정"
      exports: ["GMAIL_CREDENTIALS_PATH", "GMAIL_TOKEN_PATH", "NOTION_TOKEN", "NOTION_DATABASE_ID"]
    - path: ".env.example"
      provides: "환경변수 템플릿"
      contains: "NOTION_TOKEN"
    - path: ".gitignore"
      provides: "민감 파일 제외"
      contains: "token.json"
    - path: "auth.py"
      provides: "Gmail OAuth 인증 로직"
      exports: ["get_gmail_service"]
      min_lines: 30
    - path: "verify_gmail.py"
      provides: "Gmail 연결 검증 스크립트"
      min_lines: 15
  key_links:
    - from: "auth.py"
      to: "config.py"
      via: "import로 경로 설정 로드"
      pattern: "from config import"
    - from: "auth.py"
      to: "credentials.json"
      via: "InstalledAppFlow.from_client_secrets_file"
      pattern: "from_client_secrets_file"
    - from: "verify_gmail.py"
      to: "auth.py"
      via: "get_gmail_service() 호출"
      pattern: "from auth import get_gmail_service"
---

<objective>
프로젝트 스캐폴딩(requirements.txt, config.py, .env, .gitignore)을 생성하고 Gmail OAuth 2.0 인증 모듈을 구현한다.

Purpose: Phase 1의 기반이 되는 프로젝트 구조와 Gmail API 인증을 확립한다 (AUTH-01).
Output: 프로젝트 구조 파일 + auth.py (Gmail OAuth) + verify_gmail.py + 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-auth-env-setup/01-CONTEXT.md
@.planning/phases/01-auth-env-setup/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 프로젝트 스캐폴딩 생성</name>
  <files>requirements.txt, config.py, .env.example, .gitignore</files>
  <read_first>
    CLAUDE.md
    .planning/phases/01-auth-env-setup/01-CONTEXT.md
    .planning/phases/01-auth-env-setup/01-RESEARCH.md
  </read_first>
  <action>
    Per D-01: pip + requirements.txt + venv 조합 사용.
    Per D-02: 플랫 구조 (루트에 main.py, auth.py, gmail_client.py, notion_client.py, config.py + parsers/).

    1. **requirements.txt** 생성 (per D-01, RESEARCH Standard Stack 버전):
    ```
    google-auth==2.49.1
    google-auth-oauthlib==1.3.1
    google-api-python-client==2.193.0
    notion-client==3.0.0
    python-dotenv==1.2.2
    pytest==8.3.5
    pytest-mock==3.14.0
    ```

    2. **config.py** 생성 (per D-07, D-08):
    ```python
    """환경변수 로드 및 프로젝트 설정 관리"""
    import os
    from dotenv import load_dotenv

    load_dotenv()

    # Gmail OAuth 설정 (per D-08)
    GMAIL_CREDENTIALS_PATH = os.getenv("GMAIL_CREDENTIALS_PATH", "credentials.json")
    GMAIL_TOKEN_PATH = os.getenv("GMAIL_TOKEN_PATH", "token.json")

    # Gmail API 스코프 (per D-05: gmail.readonly로 시작)
    GMAIL_SCOPES = ["https://www.googleapis.com/auth/gmail.readonly"]

    # Notion 설정
    NOTION_TOKEN = os.getenv("NOTION_TOKEN")
    NOTION_DATABASE_ID = os.getenv("NOTION_DATABASE_ID")

    # 실행 설정
    CHECK_INTERVAL_HOURS = int(os.getenv("CHECK_INTERVAL_HOURS", "6"))
    ```

    3. **.env.example** 생성 (per D-07, D-08):
    ```
    # Gmail OAuth 설정
    GMAIL_CREDENTIALS_PATH=credentials.json
    GMAIL_TOKEN_PATH=token.json

    # Notion 설정
    NOTION_TOKEN=your_notion_integration_token_here
    NOTION_DATABASE_ID=your_notion_database_id_here

    # 실행 설정
    CHECK_INTERVAL_HOURS=6
    ```

    4. **.gitignore** 생성:
    ```
    # 인증 파일 (절대 커밋 금지)
    .env
    token.json
    credentials.json

    # Python
    __pycache__/
    *.pyc
    *.pyo
    .venv/
    venv/

    # IDE
    .vscode/
    .idea/

    # OS
    .DS_Store
    Thumbs.db
    ```

    5. venv 생성 및 의존성 설치:
    ```bash
    cd {project_root}
    python -m venv .venv
    .venv/Scripts/pip install -r requirements.txt   # Windows
    ```

    6. tests/ 디렉토리 생성 (빈 __init__.py 포함).
  </action>
  <verify>
    <automated>cd "C:\GDrive_CK\내 드라이브\05_Dev\get-ASAP" && .venv/Scripts/python -c "from config import GMAIL_CREDENTIALS_PATH, GMAIL_TOKEN_PATH, GMAIL_SCOPES, NOTION_TOKEN; print('config OK')"</automated>
  </verify>
  <acceptance_criteria>
    - requirements.txt contains "google-api-python-client==2.193.0"
    - requirements.txt contains "notion-client==3.0.0"
    - requirements.txt contains "python-dotenv==1.2.2"
    - config.py contains "GMAIL_CREDENTIALS_PATH = os.getenv"
    - config.py contains "GMAIL_TOKEN_PATH = os.getenv"
    - config.py contains 'GMAIL_SCOPES = ["https://www.googleapis.com/auth/gmail.readonly"]'
    - config.py contains "NOTION_TOKEN = os.getenv"
    - config.py contains "NOTION_DATABASE_ID = os.getenv"
    - config.py contains "CHECK_INTERVAL_HOURS"
    - .env.example contains "GMAIL_CREDENTIALS_PATH=credentials.json"
    - .env.example contains "GMAIL_TOKEN_PATH=token.json"
    - .env.example contains "NOTION_TOKEN="
    - .env.example contains "NOTION_DATABASE_ID="
    - .env.example contains "CHECK_INTERVAL_HOURS=6"
    - .gitignore contains "token.json"
    - .gitignore contains "credentials.json"
    - .gitignore contains ".env"
    - .gitignore contains "__pycache__/"
    - .venv directory exists
    - `pip list` shows google-api-python-client, notion-client, python-dotenv, pytest installed
  </acceptance_criteria>
  <done>requirements.txt, config.py, .env.example, .gitignore가 프로젝트 루트에 생성되고, venv에 모든 의존성이 설치됨</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gmail OAuth 인증 모듈 구현 (auth.py + verify_gmail.py)</name>
  <files>auth.py, verify_gmail.py, tests/test_gmail_auth.py, tests/conftest.py</files>
  <read_first>
    config.py
    .planning/phases/01-auth-env-setup/01-RESEARCH.md
  </read_first>
  <behavior>
    - test_get_gmail_service_with_valid_token: 유효한 token.json이 있으면 Gmail 서비스 객체 반환 (Credentials mock)
    - test_get_gmail_service_refreshes_expired_token: 만료된 토큰이면 creds.refresh() 호출 후 token.json 재저장
    - test_get_gmail_service_no_token_triggers_flow: token.json 없으면 InstalledAppFlow 실행
    - test_config_imports: auth.py가 config에서 경로/스코프를 import
  </behavior>
  <action>
    Per D-03, D-04, D-05, D-06: Desktop App OAuth, 브라우저 인증, gmail.readonly 스코프, 토큰 자동갱신.

    1. **tests/conftest.py** 생성:
    ```python
    """공유 테스트 픽스처"""
    import pytest
    import os

    @pytest.fixture
    def mock_env(monkeypatch):
        """테스트용 환경변수 설정"""
        monkeypatch.setenv("GMAIL_CREDENTIALS_PATH", "test_credentials.json")
        monkeypatch.setenv("GMAIL_TOKEN_PATH", "test_token.json")
        monkeypatch.setenv("NOTION_TOKEN", "test_notion_token")
        monkeypatch.setenv("NOTION_DATABASE_ID", "test_db_id")
    ```

    2. **tests/test_gmail_auth.py** 생성:
    - `test_get_gmail_service_with_valid_token`: mock Credentials(valid=True) -> build() 호출 확인
    - `test_get_gmail_service_refreshes_expired_token`: mock Credentials(valid=False, expired=True, refresh_token="x") -> creds.refresh() 호출 + token 파일 쓰기 확인
    - `test_get_gmail_service_no_token_triggers_flow`: token.json 없음 -> InstalledAppFlow.from_client_secrets_file() 호출 확인
    - `test_auth_uses_config_paths`: auth.py가 config.GMAIL_CREDENTIALS_PATH와 config.GMAIL_TOKEN_PATH 사용 확인

    3. 테스트 실행하여 RED 확인.

    4. **auth.py** 구현 (RESEARCH Pattern 1 기반, per D-05 gmail.readonly):
    ```python
    """Gmail OAuth 2.0 인증 모듈 (per D-03, D-04, D-05, D-06)"""
    import os
    from google.auth.transport.requests import Request
    from google.oauth2.credentials import Credentials
    from google_auth_oauthlib.flow import InstalledAppFlow
    from googleapiclient.discovery import build
    from config import GMAIL_CREDENTIALS_PATH, GMAIL_TOKEN_PATH, GMAIL_SCOPES


    def get_gmail_service():
        """Gmail API 서비스 객체 반환 (인증 포함)

        - token.json 존재 시 로드
        - 만료 시 자동 갱신 (per D-06)
        - 최초 실행 시 브라우저 인증 (per D-04)
        """
        creds = None

        # 저장된 토큰 로드
        if os.path.exists(GMAIL_TOKEN_PATH):
            creds = Credentials.from_authorized_user_file(GMAIL_TOKEN_PATH, GMAIL_SCOPES)

        # 유효한 자격증명이 없으면 인증/갱신
        if not creds or not creds.valid:
            if creds and creds.expired and creds.refresh_token:
                # 만료된 토큰 자동 갱신 (per D-06)
                creds.refresh(Request())
            else:
                # 최초 실행: 브라우저로 OAuth 인증 (per D-04)
                flow = InstalledAppFlow.from_client_secrets_file(
                    GMAIL_CREDENTIALS_PATH, GMAIL_SCOPES
                )
                creds = flow.run_local_server(port=0)

            # 토큰 저장 (다음 실행 시 재사용)
            with open(GMAIL_TOKEN_PATH, "w") as token:
                token.write(creds.to_json())

        return build("gmail", "v1", credentials=creds)
    ```

    5. 테스트 실행하여 GREEN 확인.

    6. **verify_gmail.py** 생성 (per D-09):
    ```python
    """Gmail API 연결 검증 스크립트 (per D-09)"""
    from auth import get_gmail_service
    from googleapiclient.errors import HttpError


    def main():
        """Gmail API 연결을 검증한다 - 라벨 목록 조회"""
        try:
            service = get_gmail_service()
            results = service.users().labels().list(userId="me").execute()
            labels = results.get("labels", [])
            print(f"Gmail 연결 성공: {len(labels)}개 라벨 확인")
            for label in labels[:5]:
                print(f"  - {label['name']}")
            if len(labels) > 5:
                print(f"  ... 외 {len(labels) - 5}개")
        except HttpError as error:
            print(f"Gmail API 오류: {error}")
            raise SystemExit(1)


    if __name__ == "__main__":
        main()
    ```
  </action>
  <verify>
    <automated>cd "C:\GDrive_CK\내 드라이브\05_Dev\get-ASAP" && .venv/Scripts/python -m pytest tests/test_gmail_auth.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - auth.py contains "def get_gmail_service():"
    - auth.py contains "from config import GMAIL_CREDENTIALS_PATH, GMAIL_TOKEN_PATH, GMAIL_SCOPES"
    - auth.py contains "InstalledAppFlow.from_client_secrets_file"
    - auth.py contains "creds.refresh(Request())"
    - auth.py contains 'build("gmail", "v1", credentials=creds)'
    - auth.py contains "creds.to_json()"
    - verify_gmail.py contains "from auth import get_gmail_service"
    - verify_gmail.py contains 'labels().list(userId="me")'
    - tests/test_gmail_auth.py exits 0 with all tests passing
    - tests/conftest.py contains "mock_env"
  </acceptance_criteria>
  <done>auth.py가 Gmail OAuth 인증/갱신/저장을 처리하고, verify_gmail.py가 연결을 검증하며, 단위 테스트가 모두 통과함</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Gmail OAuth 브라우저 인증 실행</name>
  <files>credentials.json, token.json, .env</files>
  <action>
    사용자가 Google Cloud Console에서 credentials.json을 다운로드하고,
    브라우저에서 OAuth 인증을 완료하여 token.json을 생성한다.
    이 작업은 브라우저 기반 Google 로그인이 필요하므로 Claude가 자동화할 수 없다 (per D-04).

    사전 준비 (Google Cloud Console):
    1. Google Cloud Console (https://console.cloud.google.com) 에서 프로젝트 생성 또는 선택
    2. APIs and Services > Library > "Gmail API" 검색 > Enable
    3. APIs and Services > OAuth consent screen > External 선택 > 앱 이름 입력 > 테스트 사용자에 본인 이메일 추가
    4. APIs and Services > Credentials > Create Credentials > OAuth client ID > "Desktop app" 선택 > Download JSON
    5. 다운로드한 JSON을 프로젝트 루트에 credentials.json으로 저장

    인증 실행:
    1. 프로젝트 루트에서 .env 파일 생성 (.env.example 복사 후 값 입력)
    2. 실행: `.venv\Scripts\python verify_gmail.py`
    3. 브라우저가 열리면 Google 계정 로그인 > "Allow" 클릭
    4. "Gmail 연결 성공: N개 라벨 확인" 메시지 확인
  </action>
  <verify>
    <automated>cd "C:\GDrive_CK\내 드라이브\05_Dev\get-ASAP" && test -f token.json && echo "token.json exists"</automated>
  </verify>
  <acceptance_criteria>
    - token.json 파일이 프로젝트 루트에 존재
    - verify_gmail.py 실행 시 "Gmail 연결 성공" 메시지 출력
  </acceptance_criteria>
  <done>credentials.json 배치 완료, OAuth 브라우저 인증 완료, token.json 생성됨, verify_gmail.py로 Gmail API 연결 확인됨</done>
</task>

</tasks>

<verification>
- `pytest tests/test_gmail_auth.py -x -v` 전체 통과
- config.py에서 .env 환경변수 로드 확인
- .gitignore에 token.json, credentials.json, .env 포함
- verify_gmail.py 실행 시 Gmail 라벨 목록 출력
</verification>

<success_criteria>
1. 프로젝트 스캐폴딩이 완성되고 venv에 모든 의존성 설치됨
2. auth.py의 get_gmail_service()가 Gmail API 서비스 객체를 반환함
3. token.json이 생성되고 자동 갱신 로직이 테스트로 검증됨
4. verify_gmail.py로 실제 Gmail API 연결이 확인됨
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-env-setup/01-01-SUMMARY.md`
</output>
