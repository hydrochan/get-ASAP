---
phase: 01-auth-env-setup
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - notion_auth.py
  - verify_notion.py
  - tests/test_notion_auth.py
autonomous: false
requirements:
  - AUTH-02
user_setup:
  - service: notion
    why: "Notion API Integration Token 발급"
    env_vars:
      - name: NOTION_TOKEN
        source: "Notion > Settings & members > My connections > Develop or manage integrations > New integration > Internal > Copy secret"
    dashboard_config:
      - task: "Internal Integration 생성 및 토큰 복사"
        location: "https://www.notion.so/my-integrations > New integration"
      - task: "테스트용 Notion 페이지에 Integration 연결"
        location: "Notion 페이지 > ... 메뉴 > Connections > 생성한 Integration 선택"

must_haves:
  truths:
    - "notion_auth.py가 .env의 NOTION_TOKEN으로 Notion 클라이언트를 생성한다"
    - "NOTION_TOKEN이 없으면 명확한 에러 메시지를 출력한다"
    - "verify_notion.py 실행 시 Notion 워크스페이스 이름이 출력된다"
    - ".env 파일에 모든 인증 정보가 저장되고 코드에 하드코딩된 키가 없다"
  artifacts:
    - path: "notion_auth.py"
      provides: "Notion API 클라이언트 생성 및 연결 검증"
      exports: ["get_notion_client", "verify_notion_connection"]
      min_lines: 20
    - path: "verify_notion.py"
      provides: "Notion 연결 검증 스크립트"
      min_lines: 15
    - path: "tests/test_notion_auth.py"
      provides: "Notion 인증 단위 테스트"
      min_lines: 30
  key_links:
    - from: "notion_auth.py"
      to: "config.py"
      via: "import로 NOTION_TOKEN 로드"
      pattern: "from config import"
    - from: "verify_notion.py"
      to: "notion_auth.py"
      via: "get_notion_client() 호출"
      pattern: "from notion_auth import"
    - from: "notion_auth.py"
      to: "notion-client SDK"
      via: "Client(auth=token)"
      pattern: "Client\\(auth="
---

<objective>
Notion Integration Token 기반 인증 모듈을 구현하고 API 연결을 검증한다.

Purpose: Notion API에 안정적으로 연결하여 Phase 4에서 논문 데이터를 저장할 수 있는 기반을 마련한다 (AUTH-02).
Output: notion_auth.py + verify_notion.py + 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-auth-env-setup/01-CONTEXT.md
@.planning/phases/01-auth-env-setup/01-RESEARCH.md
@.planning/phases/01-auth-env-setup/01-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 생성된 config.py 인터페이스 -->
From config.py:
```python
GMAIL_CREDENTIALS_PATH: str  # os.getenv("GMAIL_CREDENTIALS_PATH", "credentials.json")
GMAIL_TOKEN_PATH: str        # os.getenv("GMAIL_TOKEN_PATH", "token.json")
GMAIL_SCOPES: list[str]      # ["https://www.googleapis.com/auth/gmail.readonly"]
NOTION_TOKEN: str | None     # os.getenv("NOTION_TOKEN")
NOTION_DATABASE_ID: str | None  # os.getenv("NOTION_DATABASE_ID")
CHECK_INTERVAL_HOURS: int    # int(os.getenv("CHECK_INTERVAL_HOURS", "6"))
```

From tests/conftest.py:
```python
@pytest.fixture
def mock_env(monkeypatch):
    """테스트용 환경변수 설정 - NOTION_TOKEN="test_notion_token" 등"""
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Notion 인증 모듈 구현 (notion_auth.py + verify_notion.py)</name>
  <files>notion_auth.py, verify_notion.py, tests/test_notion_auth.py</files>
  <read_first>
    config.py
    tests/conftest.py
    .planning/phases/01-auth-env-setup/01-RESEARCH.md
  </read_first>
  <behavior>
    - test_get_notion_client_success: NOTION_TOKEN 환경변수가 설정되면 Client 객체 반환
    - test_get_notion_client_no_token_raises: NOTION_TOKEN이 없으면 ValueError("NOTION_TOKEN 환경변수가 설정되지 않았습니다") 발생
    - test_verify_notion_connection_success: notion.users.me() 성공 시 워크스페이스 이름 반환
    - test_verify_notion_connection_failure: APIResponseError 발생 시 ConnectionError 발생
    - test_no_hardcoded_token: notion_auth.py 소스코드에 "ntn_" 또는 "secret_" 문자열이 없음
  </behavior>
  <action>
    Per D-07, D-08: .env에서 NOTION_TOKEN 로드. Per D-10: verify_notion.py로 독립 검증.

    1. **tests/test_notion_auth.py** 생성:
    ```python
    """Notion 인증 모듈 테스트 (AUTH-02)"""
    import pytest
    from unittest.mock import MagicMock, patch


    def test_get_notion_client_success(mock_env):
        """NOTION_TOKEN이 설정되면 Client 객체를 반환한다"""
        with patch("notion_auth.Client") as mock_client:
            mock_client.return_value = MagicMock()
            from notion_auth import get_notion_client
            # reload 필요할 수 있음 - importlib.reload
            client = get_notion_client()
            mock_client.assert_called_once()


    def test_get_notion_client_no_token_raises(monkeypatch):
        """NOTION_TOKEN이 없으면 ValueError를 발생시킨다"""
        monkeypatch.delenv("NOTION_TOKEN", raising=False)
        # config 모듈 재로드하여 환경변수 변경 반영
        import importlib
        import config
        importlib.reload(config)
        from notion_auth import get_notion_client
        with pytest.raises(ValueError, match="NOTION_TOKEN"):
            get_notion_client()


    def test_verify_notion_connection_success():
        """users.me() 성공 시 워크스페이스 이름을 반환한다"""
        mock_client = MagicMock()
        mock_client.users.me.return_value = {
            "type": "bot",
            "bot": {"workspace_name": "Test Workspace"}
        }
        from notion_auth import verify_notion_connection
        result = verify_notion_connection(mock_client)
        assert result == "Test Workspace"


    def test_verify_notion_connection_failure():
        """API 오류 시 ConnectionError를 발생시킨다"""
        from notion_client import APIResponseError
        mock_client = MagicMock()
        # APIResponseError는 실제 response 객체 필요 - mock으로 처리
        mock_client.users.me.side_effect = APIResponseError(
            MagicMock(status=401), "", ""
        )
        from notion_auth import verify_notion_connection
        with pytest.raises(ConnectionError, match="Notion API 연결 실패"):
            verify_notion_connection(mock_client)


    def test_no_hardcoded_token():
        """notion_auth.py에 토큰이 하드코딩되지 않았는지 확인"""
        import pathlib
        source = pathlib.Path("notion_auth.py").read_text()
        assert "ntn_" not in source, "Notion 토큰이 하드코딩됨"
        assert "secret_" not in source, "비밀 토큰이 하드코딩됨"
    ```

    2. 테스트 실행하여 RED 확인 (notion_auth.py 없으므로 import error).

    3. **notion_auth.py** 구현 (RESEARCH Pattern 2 기반):
    ```python
    """Notion API 인증 모듈 (per D-07, D-08, D-10)"""
    from notion_client import Client, APIResponseError
    from config import NOTION_TOKEN


    def get_notion_client() -> Client:
        """Notion API 클라이언트 반환

        .env의 NOTION_TOKEN 환경변수를 사용한다 (per D-07).
        토큰이 없으면 ValueError를 발생시킨다.
        """
        if not NOTION_TOKEN:
            raise ValueError(
                "NOTION_TOKEN 환경변수가 설정되지 않았습니다. "
                ".env 파일에 NOTION_TOKEN=your_token 을 추가하세요."
            )
        return Client(auth=NOTION_TOKEN)


    def verify_notion_connection(client: Client) -> str:
        """Notion API 연결 검증 - 워크스페이스 이름 반환

        GET /v1/users/me 엔드포인트로 봇 사용자 정보를 조회한다.
        """
        try:
            bot_info = client.users.me()
            workspace_name = bot_info.get("bot", {}).get("workspace_name", "Unknown")
            return workspace_name
        except APIResponseError as e:
            raise ConnectionError(f"Notion API 연결 실패: {e}")
    ```

    4. 테스트 실행하여 GREEN 확인.

    5. **verify_notion.py** 생성 (per D-10):
    ```python
    """Notion API 연결 검증 스크립트 (per D-10)"""
    from notion_auth import get_notion_client, verify_notion_connection


    def main():
        """Notion API 연결을 검증한다 - 워크스페이스 정보 조회"""
        try:
            client = get_notion_client()
            workspace_name = verify_notion_connection(client)
            print(f"Notion 연결 성공: 워크스페이스 '{workspace_name}'")
        except ValueError as e:
            print(f"설정 오류: {e}")
            raise SystemExit(1)
        except ConnectionError as e:
            print(f"연결 오류: {e}")
            raise SystemExit(1)


    if __name__ == "__main__":
        main()
    ```

    참고: notion_auth.py에서 config.NOTION_TOKEN을 모듈 레벨에서 import하므로,
    테스트에서 환경변수 변경 후 config와 notion_auth를 importlib.reload() 해야 할 수 있다.
    테스트 코드에서 이 점을 반영할 것.
  </action>
  <verify>
    <automated>cd "C:\GDrive_CK\내 드라이브\05_Dev\get-ASAP" && .venv/Scripts/python -m pytest tests/test_notion_auth.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - notion_auth.py contains "def get_notion_client()"
    - notion_auth.py contains "def verify_notion_connection("
    - notion_auth.py contains "from config import NOTION_TOKEN"
    - notion_auth.py contains "Client(auth=NOTION_TOKEN)"
    - notion_auth.py contains 'raise ValueError' with "NOTION_TOKEN" in the message
    - notion_auth.py contains "client.users.me()"
    - notion_auth.py does NOT contain "ntn_" or "secret_" (no hardcoded tokens)
    - verify_notion.py contains "from notion_auth import get_notion_client, verify_notion_connection"
    - verify_notion.py contains 'print(f"Notion 연결 성공'
    - tests/test_notion_auth.py exits 0 with all tests passing
  </acceptance_criteria>
  <done>notion_auth.py가 NOTION_TOKEN으로 클라이언트를 생성하고, verify_notion.py가 연결을 검증하며, 단위 테스트가 모두 통과함</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Notion 연결 검증 및 전체 Phase 1 확인</name>
  <files>.env</files>
  <action>
    사용자가 Notion Integration을 생성하고 NOTION_TOKEN을 .env에 설정한 뒤,
    verify_notion.py와 verify_gmail.py를 실행하여 양쪽 API 연결을 확인한다.

    사전 준비 (Notion Integration):
    1. https://www.notion.so/my-integrations 접속
    2. "New integration" 클릭 > 이름: "get-ASAP" > "Internal" 선택 > Submit
    3. "Internal Integration Secret" 복사
    4. .env 파일의 NOTION_TOKEN= 뒤에 복사한 토큰 붙여넣기
    5. (선택) 테스트용 Notion 페이지 생성 > "..." > "Connections" > 생성한 Integration 연결

    검증 단계:
    1. `.venv\Scripts\python verify_notion.py` -- "Notion 연결 성공: 워크스페이스 'xxx'" 출력
    2. `.venv\Scripts\python verify_gmail.py` -- "Gmail 연결 성공: N개 라벨 확인" 출력
    3. `.venv\Scripts\python -m pytest tests/ -v` -- 모든 테스트 통과
    4. `git status` -- token.json, credentials.json, .env가 tracked되지 않음 확인
  </action>
  <verify>
    <automated>cd "C:\GDrive_CK\내 드라이브\05_Dev\get-ASAP" && .venv/Scripts/python -m pytest tests/ -v</automated>
  </verify>
  <acceptance_criteria>
    - verify_notion.py 실행 시 "Notion 연결 성공" 문자열 출력
    - verify_gmail.py 실행 시 "Gmail 연결 성공" 문자열 출력
    - pytest tests/ 전체 통과 (exit code 0)
    - .gitignore에 의해 token.json, credentials.json, .env가 git tracked되지 않음
  </acceptance_criteria>
  <done>Gmail과 Notion 양쪽 API 연결이 성공하고, 모든 테스트가 통과하며, 민감 파일이 .gitignore로 보호됨</done>
</task>

</tasks>

<verification>
- `pytest tests/ -v` 전체 통과 (test_gmail_auth.py + test_notion_auth.py)
- verify_gmail.py 실행 시 "Gmail 연결 성공" 출력
- verify_notion.py 실행 시 "Notion 연결 성공" 출력
- auth.py, notion_auth.py에 하드코딩된 토큰 없음
- .gitignore에 token.json, credentials.json, .env 포함
</verification>

<success_criteria>
1. notion_auth.py의 get_notion_client()가 NOTION_TOKEN으로 클라이언트를 생성함
2. verify_notion.py로 실제 Notion API 연결이 확인됨
3. Gmail + Notion 양쪽 API 연결이 모두 성공함
4. 모든 단위 테스트가 통과함
5. 코드에 하드코딩된 API 키가 없고 .env로 관리됨
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-env-setup/01-02-SUMMARY.md`
</output>
